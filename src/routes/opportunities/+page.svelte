<script lang="ts">
  import { t } from '$lib/i18n';
  // Redirects to /projects?tab=needs at load time (see +page.ts).
</script>

<p class="muted">{$t('Loading…')} <a href="/projects?tab=needs">{$t('Open needs')} →</a></p>
